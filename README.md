## Текстовый файловый менеджер на чистом JavaScript

Одностраничное веб‑приложение, имитирующее простую текстовую файловую систему (папки и файлы) с навигацией, редактированием текстовых файлов и сохранением состояния в `localStorage`.  
Проект реализуется **без фреймворков**, только на:

- **HTML**
- **CSS**
- **Vanilla JavaScript**
- **DOM API**
- **localStorage**

---
 
### Функциональные возможности

- **Файловая система как дерево**:
  - Папки и файлы с уникальными `id`.
  - Вложенность папок любой глубины.
  - Структура целиком хранится в `localStorage`.
- **Навигация по папкам**:
  - Переход внутрь папки по клику.
  - Отображение текущего пути (breadcrumb).
- **Работа с файлами**:
  - Создание текстовых файлов.
  - Просмотр и редактирование содержимого.
  - Сохранение даты создания `createdAt`.
- **Работа с папками**:
  - Создание вложенных папок.
  - Переименование.
  - Удаление с подтверждением.
- **Общие операции**:
  - Переименование файлов и папок.
  - Валидация имён.
  - Поиск по имени по всей файловой системе.
  - Отображение статистики по файловой системе.
- **Сохранение состояния**:
  - Все изменения сохраняются в `localStorage`.
  - После перезагрузки страницы структура сохраняется.
- **Адаптивный интерфейс**:
  - На ПК — сетка карточек.
  - На телефоне — вертикальный список.

---

### Технологические ограничения

- Без библиотек и фреймворков (`React`, `Vue`, `jQuery` и т.п. не используются).
- Только работа с DOM через стандартный **DOM API**.
- Вся логика работы с данными (файловой системой) отделена от логики работы с DOM.

---

### Структура проекта


- `index.html` — разметка страницы и базовые элементы интерфейса.
- `style.css` — оформление и адаптивная верстка.
- `filesystem.js` — функции работы с древовидной структурой файловой системы (без DOM).
- `storage.js` — обёртки над `localStorage` для сохранения/загрузки дерева.
- `ui.js` — отображение данных на странице (рендер папок, путь, модальные окна).
- `script.js` — основная логика приложения: навигация, обработка событий, связь между `filesystem.js`, `storage.js` и `ui.js`.

---

### Архитектура данных

Файловая система представлена в виде дерева объектов. Корень:

```json
{
  "id": "root",
  "name": "root",
  "type": "folder",
  "children": []
}
```

**Папка**:

```json
{
  "id": "string",
  "name": "string",
  "type": "folder",
  "children": []
}
```

**Файл**:

```json
{
  "id": "string",
  "name": "string",
  "type": "file",
  "content": "string",
  "createdAt": "string"
}
```

Все дерево файловой системы целиком сохраняется в `localStorage` в сериализованном виде (`JSON.stringify`).

---

### Логика в `filesystem.js`

Модуль отвечает только за **данные** (никакого DOM и прямой работы с `localStorage`):

- **`createRoot()`** — создаёт корневую папку `root`.
- **`createFile(name, content)`** — создаёт объект файла по заданному имени и содержимому.
- **`createFolder(name)`** — создаёт объект папки по заданному имени.
- **`findById(id, node)`** — рекурсивный поиск элемента в дереве по `id`, начиная с узла `node`.
- **`addItem(parentId, item, fs)`** — добавляет файл или папку `item` в папку с `parentId` в дереве `fs`.
- **`deleteItem(id, fs)`** — удаляет элемент с указанным `id` из дерева.
- **`renameItem(id, newName, fs)`** — переименовывает элемент с `id` в `newName`.
- **`countStats(node)`** — вычисляет статистику по дереву:
  - количество файлов;
  - количество папок;
  - общее число символов во всех файлах;
  - самый большой файл (по количеству символов).

---

### Логика в `storage.js`

Модуль для общения с `localStorage`:

- **`saveFS(fs)`** — сериализует дерево файловой системы и сохраняет в `localStorage`.
- **`loadFS()`** — загружает дерево из `localStorage`.  
  Если данных нет, создаёт новый `root` через `createRoot()` и сохраняет его.

---

### Интерфейс (`index.html`)

На странице должны присутствовать:

- **Верхняя панель**:
  - отображение текущего пути (набор кликабельных сегментов);
  - кнопки:
    - «Создать файл»
    - «Создать папку»
    - «Поиск»
    - «Статистика».
- **Область отображения**:
  - карточки папок и файлов:
    - 📁 — папка;
    - 📄 — файл.
- **Модальное окно** для:
  - создания файла/папки;
  - редактирования содержимого файла;
  - переименования.

---

### Логика отображения в `ui.js`

Отвечает только за работу с DOM:

- **`renderFolder(folder)`** — отрисовывает содержимое текущей папки (список/сетка карточек).
- **`renderPath(pathArray)`** — отображает текущий путь от `root` до текущей папки.
- **`openModal(type, data)`** — открывает модальное окно:
  - `type`: создание файла/папки, редактирование, переименование, поиск, статистика;
  - `data`: дополнительные данные (например, текущий элемент).
- **`closeModal()`** — закрывает модальное окно и сбрасывает состояние.

---

### Основная логика в `script.js`

- Хранит **`currentFolderId`** — `id` текущей открытой папки.
- Загружает файловую систему через `loadFS()`.
- Обрабатывает:
  - клики по папкам (смена `currentFolderId` и перерисовка);
  - открытие файлов для редактирования;
  - создание/удаление/переименование файлов и папок;
  - запуск поиска и показ результатов;
  - показ статистики по дереву (`countStats`).
- После каждого изменения:
  - обновляет DOM (через функции из `ui.js`);
  - сохраняет новое состояние в `localStorage` (`saveFS`).

---

### Валидация

При создании и переименовании:

- **Имя не пустое** (не допускаются строки из одних пробелов).
- **Имя не дублируется** среди элементов в текущей папке.
- **Ограничение длины имени**, например **до 20 символов**.

При удалении любого элемента (файла или папки) — **обязательное подтверждение** (диалог).

---

### Поиск

- Поиск по имени среди **всех** файлов и папок (рекурсивный обход дерева).
- Может быть реализован как модальное окно с полем ввода.
- Результаты:
  - список найденных элементов с указанием пути;
  - по клику — переход к папке/файлу.

---

### Статистика

Кнопка «Статистика» запускает функцию `countStats` и показывает:

- количество файлов;
- количество папок;
- общее количество символов во всех файлах;
- самый большой файл (имя, размер в символах).

---

### Адаптивность интерфейса

- **Десктоп**:
  - область содержимого — **сетка карточек**.
- **Мобильные устройства**:
  - область содержимого — **вертикальный список**.
- Используются CSS‑медиа‑запросы (`@media`) и гибкая вёрстка (flex/grid).

---

### Как запустить проект

1. Склонировать репозиторий или скопировать файлы в папку.
2. Убедиться, что структура проекта соответствует:
   - `index.html`
   - `style.css`
   - `filesystem.js`
   - `storage.js`
   - `ui.js`
   - `script.js`
3. Открыть файл `index.html` в браузере (двойной клик или через локальный сервер).
4. Проверить работу:
   - создание папки;
   - создание файла;
   - вложенность папок;
   - навигация по папкам;
   - редактирование файла;
   - переименование;
   - удаление;
   - поиск;
   - сохранение данных после перезагрузки страницы.

---

### Возможные улучшения

- Поддержка drag&drop для перемещения файлов и папок.
- Добавление контекстного меню (ПКМ по карточкам).
- Поддержка нескольких корней (несколько «дисков»).
- Экспорт/импорт файловой системы в JSON.

---

### План коммитов в репозитории

В репозитории проект разбит на несколько осмысленных коммитов:

1. Добавлен подробный `README.md`.
2. Базовая HTML‑разметка (`index.html`).
3. Стили и адаптивная вёрстка (`style.css`).
4. Логика файловой системы (`filesystem.js`).
5. Модуль работы с `localStorage` (`storage.js`).
6. Логика UI и модальные окна (`ui.js`).
7. Основная логика приложения (`script.js`).
8–10. Небольшие доработки и уточнения в документации и коде.

